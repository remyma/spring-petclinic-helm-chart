apiVersion: v1
kind: Service
metadata:
  name: {{ template "petclinic-chart.fullname" . }}
  labels:
    app: {{ template "petclinic-chart.name" . }}
    chart: {{ template "petclinic-chart.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "petclinic-chart.name" . }}
    release: {{ .Release.Name }}
